# Root-level catch-all. Each model folder under QIDI/ also carries its own
# .gitignore (deliberately - see the root README on why nothing is shared
# between models), so this only needs to cover things that could land at the
# repo root itself.
__pycache__/
*.pyc
dist/
build/
*.spec
*.exe
*.zip
